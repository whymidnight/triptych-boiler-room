export interface UserData {
  id: string;
  name: string;
  username: string;
  profile_image_url: string;
}

export interface SpaceData {
  id: string;
  participantCount: number;
  state: string;
  title: string;
}
